{
  "id": "staFacturasOCR01",
  "name": "Facturas por OCR (Telegram → LlamaCloud → STA)",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "minutes",
              "minutesInterval": 1
            }
          ]
        }
      },
      "name": "Cada minuto",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        -620,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "\n// Long-polling de Telegram. El offset vive en la static data del workflow, asi\n// que sobrevive reinicios y cortes de luz: cada mensaje se procesa UNA vez.\n//\n// POR QUE POLLING Y NO EL NODO \"Telegram Trigger\": ese nodo registra un webhook\n// y Telegram necesita ENTRAR a n8n desde internet. S1 esta detras del router del\n// local, sin IP publica. Con polling S1 SALE a internet (que ya puede) y no hay\n// que abrir puertos ni depender de un tunel que se caiga.\nconst est = $getWorkflowStaticData('global');\nconst token = $env.TELEGRAM_BOT_TOKEN;\nif (!token) throw new Error('Falta TELEGRAM_BOT_TOKEN en el entorno de n8n');\n\nconst offset = est.offset ?? 0;\nconst resp = await this.helpers.httpRequest({\n  url: `https://api.telegram.org/bot${token}/getUpdates`,\n  qs: { offset, timeout: 0, allowed_updates: JSON.stringify(['message']) },\n  json: true,\n});\nif (!resp.ok) throw new Error('getUpdates fallo: ' + JSON.stringify(resp));\n\nconst permitidos = String($env.TELEGRAM_ALLOWED_IDS || '')\n  .split(',').map((s) => s.trim()).filter(Boolean);\n\nconst out = [];\nlet maxId = offset - 1;\nfor (const up of resp.result || []) {\n  maxId = Math.max(maxId, up.update_id);\n  const msg = up.message;\n  if (!msg) continue;\n  const chatId = msg.chat?.id;\n  const fromId = String(msg.from?.id ?? '');\n  // Whitelist. Sin lista configurada NO se acepta a nadie: un bot de Telegram\n  // es publico y cualquiera que adivine el nombre puede escribirle.\n  if (!permitidos.length || !permitidos.includes(fromId)) continue;\n\n  let fileId = null;\n  if (Array.isArray(msg.photo) && msg.photo.length) {\n    fileId = msg.photo[msg.photo.length - 1].file_id;   // mayor resolucion\n  } else if (msg.document) {\n    const mt = msg.document.mime_type || '';\n    if (mt.startsWith('image/') || mt === 'application/pdf') fileId = msg.document.file_id;\n  }\n  out.push({ json: { chatId, fromId, fileId, sinArchivo: !fileId } });\n}\n// El offset avanza SIEMPRE, aun si un mensaje falla despues: si no, una factura\n// ilegible se reprocesaria en loop cada minuto para siempre.\nest.offset = maxId + 1;\nreturn out;\n"
      },
      "name": "Traer mensajes",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -400,
        300
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "version": 2
          },
          "conditions": [
            {
              "id": "c1",
              "leftValue": "={{ $json.sinArchivo }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "false",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        }
      },
      "name": "¿Trae archivo?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        -180,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "\n// getFile devuelve un file_path; el binario se baja de /file/bot<token>/<path>.\n// Se hace en un Code node (y no en dos HTTP Request) para tener el buffer en\n// memoria y calcular el sha256 sin pasar por disco.\nconst token = $env.TELEGRAM_BOT_TOKEN;\nconst out = [];\nfor (const item of $input.all()) {\n  const { fileId, chatId } = item.json;\n  const meta = await this.helpers.httpRequest({\n    url: `https://api.telegram.org/bot${token}/getFile`,\n    qs: { file_id: fileId }, json: true,\n  });\n  if (!meta.ok) throw new Error('getFile fallo: ' + JSON.stringify(meta));\n  const filePath = meta.result.file_path;\n  const buf = await this.helpers.httpRequest({\n    url: `https://api.telegram.org/file/bot${token}/${filePath}`,\n    encoding: 'arraybuffer',\n  });\n  const bin = Buffer.from(buf);\n  const crypto = require('crypto');\n  const hash = crypto.createHash('sha256').update(bin).digest('hex');\n  const nombre = filePath.split('/').pop() || 'factura';\n  const mime = nombre.toLowerCase().endsWith('.pdf') ? 'application/pdf' : 'image/jpeg';\n  out.push({\n    json: { chatId, hash, nombre, mime },\n    binary: { data: await this.helpers.prepareBinaryData(bin, nombre, mime) },\n  });\n}\nreturn out;\n"
      },
      "name": "Bajar archivo",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        60,
        200
      ]
    },
    {
      "parameters": {
        "operation": "extract",
        "configMode": "schema",
        "dataSchema": "{\n  \"type\": \"object\",\n  \"properties\": {\n    \"proveedor\": {\n      \"type\": \"object\",\n      \"description\": \"Quien EMITE la factura (el proveedor). NO es Santa Teresita Pastas, que es el receptor.\",\n      \"properties\": {\n        \"nombre\": {\n          \"type\": \"string\",\n          \"description\": \"Razon social o nombre de fantasia del emisor\"\n        },\n        \"cuit\": {\n          \"type\": \"string\",\n          \"description\": \"CUIT del emisor, formato 30-12345678-9\"\n        }\n      }\n    },\n    \"comprobante\": {\n      \"type\": \"object\",\n      \"properties\": {\n        \"tipo\": {\n          \"type\": \"string\",\n          \"description\": \"Exactamente uno de: FACTURA_A, FACTURA_B, FACTURA_C, FACTURA_X, NOTA_CREDITO, NOTA_DEBITO, TICKET, REMITO, OTRO\"\n        },\n        \"puntoVenta\": {\n          \"type\": \"string\",\n          \"description\": \"Punto de venta, 4 digitos (la primera mitad del numero, antes del guion)\"\n        },\n        \"numero\": {\n          \"type\": \"string\",\n          \"description\": \"Numero del comprobante, 8 digitos, SIN el punto de venta\"\n        },\n        \"fechaEmision\": {\n          \"type\": \"string\",\n          \"description\": \"Fecha de emision en formato YYYY-MM-DD\"\n        },\n        \"fechaVencimiento\": {\n          \"type\": \"string\",\n          \"description\": \"Fecha de vencimiento del pago en formato YYYY-MM-DD, solo si figura\"\n        }\n      }\n    },\n    \"montos\": {\n      \"type\": \"object\",\n      \"description\": \"Importes en pesos argentinos, como numeros (sin simbolo, sin separador de miles)\",\n      \"properties\": {\n        \"neto\": {\n          \"type\": \"number\",\n          \"description\": \"Importe neto gravado, sin IVA\"\n        },\n        \"iva21\": {\n          \"type\": \"number\",\n          \"description\": \"IVA al 21%, si esta discriminado\"\n        },\n        \"iva10_5\": {\n          \"type\": \"number\",\n          \"description\": \"IVA al 10,5%, si esta discriminado\"\n        },\n        \"ivaTotal\": {\n          \"type\": \"number\",\n          \"description\": \"IVA total. Si no esta discriminado por alicuota, poner el IVA total aca\"\n        },\n        \"total\": {\n          \"type\": \"number\",\n          \"description\": \"Importe TOTAL a pagar, con IVA e impuestos incluidos\"\n        }\n      }\n    },\n    \"items\": {\n      \"type\": \"array\",\n      \"description\": \"Renglones del detalle de la factura\",\n      \"items\": {\n        \"type\": \"object\",\n        \"properties\": {\n          \"descripcion\": {\n            \"type\": \"string\",\n            \"description\": \"Descripcion del articulo\"\n          },\n          \"cantidad\": {\n            \"type\": \"number\"\n          },\n          \"unidad\": {\n            \"type\": \"string\",\n            \"description\": \"kg, unidad, bolsa, caja, litro, docena...\"\n          },\n          \"precioUnitario\": {\n            \"type\": \"number\"\n          },\n          \"alicuotaIva\": {\n            \"type\": \"number\",\n            \"description\": \"21, 10.5 o 27\"\n          },\n          \"subtotal\": {\n            \"type\": \"number\",\n            \"description\": \"Importe del renglon\"\n          }\n        }\n      }\n    }\n  }\n}",
        "inputType": "binaryFile",
        "inputDataFieldName": "data"
      },
      "name": "OCR (LlamaCloud)",
      "type": "@llamaindex/n8n-nodes-llamacloud.llamaParsePlatform",
      "typeVersion": 1,
      "position": [
        280,
        200
      ],
      "credentials": {
        "llamaParseApi": {
          "id": "staLlamaParse001",
          "name": "LlamaParse API"
        }
      },
      "onError": "continueRegularOutput",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000,
      "notes": "Nodo oficial @llamaindex/n8n-nodes-llamacloud (v6.7.x). Sube el binario, crea el job en /api/v2/extract con el schema inline y poletea hasta COMPLETED. Devuelve { result: \"<json stringificado>\" }."
    },
    {
      "parameters": {
        "jsCode": "\n// Mapea la salida del OCR al contrato de POST /api/v1/ingest/facturas.\n// El contrato del programa SI esta verificado (apps/api/src/routes/ingest.ts).\n//\n// El nodo de LlamaCloud devuelve SOLO { result: \"<json stringificado>\" } — no\n// arrastra el chatId ni el hash. Se recuperan de \"Bajar archivo\" por\n// pairedItem: el nodo emite exactamente un item por entrada, en orden, incluso\n// cuando falla (onError: continueRegularOutput).\nconst bajados = $('Bajar archivo').all();\nconst entradas = $input.all();\nconst out = [];\n\nfor (let i = 0; i < entradas.length; i++) {\n  const item = entradas[i];\n  const par = item.pairedItem;\n  const idx = typeof par === 'number' ? par : (par?.item ?? i);\n  const previo = bajados[idx]?.json ?? bajados[i]?.json ?? {};\n  const chatId = previo.chatId;\n  const hash = previo.hash;\n\n  let crudo = null;\n  let motivoFallo = item.json.error ? String(item.json.error) : null;\n  if (!motivoFallo) {\n    try {\n      crudo = typeof item.json.result === 'string'\n        ? JSON.parse(item.json.result)\n        : (item.json.result ?? null);\n    } catch (e) {\n      motivoFallo = 'el OCR devolvio algo que no es JSON';\n    }\n  }\n\n  // El schema del nodo devuelve la forma de abajo tal cual; el `?? crudo.data`\n  // es red por si LlamaCloud llegara a envolver el resultado.\n  const d = crudo?.data ?? crudo ?? {};\n  const leer = (...rutas) => {\n    for (const r of rutas) {\n      let v = d;\n      for (const k of r.split('.')) v = v?.[k];\n      if (v !== undefined && v !== null && v !== '') return v;\n    }\n    return undefined;\n  };\n  const num = (v) => {\n    if (v === undefined || v === null || v === '') return undefined;\n    if (typeof v === 'number') return Number.isFinite(v) ? v : undefined;\n    const n = Number(String(v).replace(/[^0-9.,-]/g, '').replace(/\\.(?=\\d{3}\\b)/g, '').replace(',', '.'));\n    return Number.isFinite(n) ? n : undefined;\n  };\n\n  const total = num(leer('montos.total', 'total', 'importe_total'));\n  const proveedor = leer('proveedor.nombre', 'proveedor', 'razon_social', 'emisor.nombre');\n  const numero = leer('comprobante.numero', 'numero', 'nro_comprobante');\n\n  const problemas = [];\n  if (motivoFallo) problemas.push('el OCR devolvio error: ' + motivoFallo);\n  if (!total || total <= 0) problemas.push('no se leyo el total');\n  if (!proveedor) problemas.push('no se leyo el proveedor');\n  if (!numero) problemas.push('no se leyo el numero de comprobante');\n\n  // Sin estos tres no se puede crear la factura: se avisa y se corta.\n  if (problemas.length) {\n    out.push({ json: { chatId, error: true, problemas } });\n    continue;\n  }\n\n  const items = (leer('items', 'detalle', 'lineas') || []).map((it) => ({\n    descripcion: String(it.descripcion ?? it.description ?? it.detalle ?? '').slice(0, 300),\n    cantidad: num(it.cantidad ?? it.quantity),\n    unidad: it.unidad ?? it.unit,\n    precioUnitario: num(it.precioUnitario ?? it.precio_unitario ?? it.unit_price),\n    alicuotaIva: num(it.alicuotaIva ?? it.iva),\n    subtotal: num(it.subtotal ?? it.importe),\n  })).filter((it) => it.descripcion);\n\n  // Descuadre: no bloquea (el humano valida igual) pero baja la confianza y\n  // deja la razon escrita para que quien revisa sepa donde mirar.\n  const suma = items.reduce((a, it) => a + (it.subtotal || 0), 0);\n  const descuadra = items.length > 0 && Math.abs(total - suma) > total * 0.05;\n\n  out.push({ json: {\n    chatId,\n    _resumen: { proveedor, total },\n    payload: {\n      adjunto: { hash },\n      proveedor: { nombre: String(proveedor).slice(0, 120), cuit: leer('proveedor.cuit', 'cuit') },\n      comprobante: {\n        tipo: leer('comprobante.tipo', 'tipo') ?? 'OTRO',\n        puntoVenta: leer('comprobante.puntoVenta', 'punto_venta'),\n        numero: String(numero).slice(0, 40),\n        fechaEmision: leer('comprobante.fechaEmision', 'fecha_emision', 'fecha')\n                      ?? new Date().toISOString().slice(0, 10),\n        fechaVencimiento: leer('comprobante.fechaVencimiento', 'fecha_vencimiento'),\n      },\n      montos: {\n        neto: num(leer('montos.neto', 'neto')),\n        ivaTotal: num(leer('montos.ivaTotal', 'iva')),\n        iva21: num(leer('montos.iva21')),\n        iva10_5: num(leer('montos.iva10_5')),\n        total,\n      },\n      items,\n      ocr: { confianza: descuadra ? 0.5 : 0.8, payload: crudo },\n      observaciones: descuadra ? 'revisar: los items no suman el total' : undefined,\n    },\n  }});\n}\nreturn out;\n"
      },
      "name": "Armar factura",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        200
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "version": 2
          },
          "conditions": [
            {
              "id": "c2",
              "leftValue": "={{ $json.error }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "false",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        }
      },
      "name": "¿Se pudo leer?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        720,
        200
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.INGEST_URL }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.payload) }}",
        "options": {
          "response": {
            "response": {
              "neverError": true,
              "fullResponse": true
            }
          }
        }
      },
      "name": "Cargar en el programa",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        940,
        120
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "staIngestaSTA001",
          "name": "Ingesta STA"
        }
      },
      "onError": "continueRegularOutput",
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000
    },
    {
      "parameters": {
        "jsCode": "\n// Un solo nodo arma TODAS las respuestas y las manda. Centralizarlo evita que\n// una rama quede muda (que es como se pierde una factura sin que nadie note).\nconst token = $env.TELEGRAM_BOT_TOKEN;\nconst plata = (n) => Number(n || 0).toLocaleString('es-AR', { minimumFractionDigits: 2 });\n\nfor (const item of $input.all()) {\n  const j = item.json;\n  const chatId = j.chatId;\n  if (!chatId) continue;\n  let texto;\n\n  if (j.sinArchivo) {\n    texto = 'Mandame una *foto* o un *PDF* de la factura.';\n  } else if (j.error) {\n    texto = '⚠️ No pude leer la factura (' + (j.problemas || []).join('; ') + ').\\n'\n          + 'Probá con una foto más nítida y derecha, o cargala a mano en el sistema.';\n  } else {\n    const code = j.statusCode;\n    const body = j.body ?? {};\n    const r = j._resumen ?? {};\n    if (code === 201) {\n      texto = `✅ Factura de *${r.proveedor}* por $${plata(r.total)} cargada.\\n`\n            + 'Queda *sin validar*: revisala y aceptala en Admin → Facturas.';\n    } else if (code === 200 && body.duplicate) {\n      texto = 'ℹ️ Esa factura ya estaba cargada.';\n    } else if (code === 503) {\n      texto = '⚠️ La ingesta está deshabilitada en el servidor (falta INGEST_API_TOKEN).';\n    } else if (code === 401) {\n      texto = '⚠️ El token de ingesta es inválido. Avisale al que administra el sistema.';\n    } else {\n      texto = `⚠️ El sistema rechazó la factura (${code}). Cargala a mano por ahora.`;\n    }\n  }\n\n  await this.helpers.httpRequest({\n    method: 'POST',\n    url: `https://api.telegram.org/bot${token}/sendMessage`,\n    body: { chat_id: chatId, text: texto, parse_mode: 'Markdown' },\n    json: true,\n  });\n}\nreturn $input.all();\n"
      },
      "name": "Responder",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1160,
        300
      ]
    }
  ],
  "connections": {
    "Cada minuto": {
      "main": [
        [
          {
            "node": "Traer mensajes",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Traer mensajes": {
      "main": [
        [
          {
            "node": "¿Trae archivo?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "¿Trae archivo?": {
      "main": [
        [
          {
            "node": "Bajar archivo",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Responder",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Bajar archivo": {
      "main": [
        [
          {
            "node": "OCR (LlamaCloud)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "OCR (LlamaCloud)": {
      "main": [
        [
          {
            "node": "Armar factura",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Armar factura": {
      "main": [
        [
          {
            "node": "¿Se pudo leer?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "¿Se pudo leer?": {
      "main": [
        [
          {
            "node": "Cargar en el programa",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Responder",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Cargar en el programa": {
      "main": [
        [
          {
            "node": "Responder",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "all",
    "timezone": "America/Argentina/Buenos_Aires"
  },
  "active": false
}
