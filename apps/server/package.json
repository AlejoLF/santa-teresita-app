{
  "name": "@sta/server",
  "version": "1.0.0",
  "private": true,
  "description": "Santa Teresita — servidor local LAN (mini PC). Postgres fuente de verdad + API + replicator a Supabase. Deliverable separado del .exe (ver docs/SERVIDOR-LOCAL.md).",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit || true"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
