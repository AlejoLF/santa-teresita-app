// Preload mínimo. La demo no necesita IPC con Node — todo vive en el WebView
// + sessionStorage. Si en el futuro se quiere ofrecer impresión nativa o file
// access, exponé APIs acá usando contextBridge.
